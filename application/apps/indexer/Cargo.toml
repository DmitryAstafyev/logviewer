[workspace]

members = [
    "indexer_base",
    "indexer_cli",
    "merging",
    "parsers",
    "processor",
    "session",
    "unbound",
    "sources",
]

# only uncomment when profiling
[profile.release]
debug = true
